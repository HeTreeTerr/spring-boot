server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server  #主机名

  client:
    register-with-eureka: false #不将自己注册进eureka去
    fetch-registry: false  #不获取注册信息
    service-url:
     defaultZone: http://localhost:8761/eureka/
