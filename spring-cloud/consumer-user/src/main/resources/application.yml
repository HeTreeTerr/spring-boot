server:
  port: 8200

spring:
  application:
    name: consumer-user

eureka:
  instance:
    prefer-ip-address: true #注册时使用服务ip地址
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/